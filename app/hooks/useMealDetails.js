// "use client";

// import { useGetCategoryQuery } from "../redux/features/api/apiSlice";
// import { useDispatch, useSelector } from "react-redux";

// export const useMealDetails = () => {
//   const categories = useSelector((state) => state.recipe.recipes);
//   // const dispatch = useDispatch();
//   const { data: category, isLoading, isSuccess, isError, error } = useGetCategoryQuery(categories);

//   if (isSuccess) {
//     console.log(category);
//   }

//   // if (Array.isArray(categories) && categories.length) {
//   //   categories[0].map((cat) => {

//   //   });
//   // }
// };
